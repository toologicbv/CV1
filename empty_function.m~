function [ output_args ] = harris_corner_detector( image_path, sgima )

kernel_lenght = (6 * sigma) + 1;
G = fspecial('guassian',[1 kernel_lenght],sigma);


im = imread(image_path);
im = im2double(im);
im = gb2grey(im);



imOut = conv2(im, G, 'same');
imOut = conv2(im, G, 'same');


end

